#include <bits/stdc++.h>
using namespace std;

vector<int> A;
map<int,int> MAP;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int N, K;
    cin >> N >> K;

    A.resize(N+1);


    for (int i = 0; i < N; i++) {
        cin >> A[i];
        if (h != 0) {=
        }
    }


}

/*
1찾았어
다른거 계속 만날때마다 거리 계산해 그리고 1을 또 찾았어. 못찾았으면 거리유지
1을 찾았으면, 거리 딱 계산됬지 거리와 K를 비교해.
그렇게 거리 배열을 만들어
거리 배열 중에서 <=K인거 K보다 작거나 같은거
거리배열이 pair야. atom1 atom2 합한 값도 가져. 그거로 max
 */