#include <iostream>
#include <vector>
#include <string>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int a, b;//동현 주원이야. 주원이 이겨야해. 극적으로 이겨야 해.
    cin >> a >> b;
    int n;
    cin >> n;
    
    for (int i = 0; i < n; i++) {
        int l, r;
        cin >> l >> r;
        
    }
}